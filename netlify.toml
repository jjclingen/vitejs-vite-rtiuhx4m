[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "18"

# Route /api/ai in the app to the serverless function
[[redirects]]
  from = "/api/ai"
  to = "/.netlify/functions/ai"
  status = 200
